{
  "buildCommand": "git submodule update --init --recursive && hugo --gc --minify --cleanDestinationDir && echo '--- public/posts ---' && ls -la public/posts || true",
  "outputDirectory": "public",
  "trailingSlash": true
}
